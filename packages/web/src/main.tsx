import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return <p>Hanabi Platform</p>;
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
